#include<stdio.h>
int main()
{
	int a[10],n=10,i,min;
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	min=a[0];
	for(i=0;i<n;i++)
		if(abs(a[i])<abs(min)) 
		min=a[0];				//min在前，顺序重要
	printf("%d",min);
	return 0;
}
