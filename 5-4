#include<stdio.h>
int main()
{
	int a[20],i,j,e;
	int n=20;
	for(i=0;i<n;i++)
		scanf("%d",&a[i]);
	for(i=0;i<n;i++)
		{
		for(j=0;j<n;j++)
			{
				if(a[i]%a[j]==0&&i!=j)	
				printf("%d ",a[i]);	break;		//break需在内循环中，加入{}
			}
		}
	return 0;
}
