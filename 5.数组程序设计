📌数组：顺序关系+若干相同类型变量的构造类型。
		**数组名+下标=a[n],n不能为变量，只能为常量或整型表达式，必须为方括号。
		**先定义，后使用
		**只能逐个引用数组元素，不能一次引用整个数组
			❌int a[10]		/*数组的容量/长度*/
				printf("%d",a);
			✅for(j=0;j<10;j++}
				printf("%d\t",a[j]);
		**数组不要越界（自己检查）
	一维数组
		初始化：在定义数组时赋初值
			int a[5]={1,2,3,4,5};  /*可少不可多，0可省略*/
			int a[]={1,2,3};	/*可以不指定长度，根据初值个数确定数组大小*/
			
	二维数组
	字符数组
📌 ##输入10个整数，找出最大值和最小值
	int a[10],i,max,min;                      //可以将两行int合并  
	int n=10;								 // 输入10个整数
	for(i=0;i<n;i++)                      	 //输入n个数，a[0]到a[n-1]
		scanf("%d ",&a[i]);
	max=min=a[0];                 			 // 初始化最大最小值
	for(i=0;i<(n-1-j);i++)					 // 查找最大值和最小值
			if (a[i]>max)	                    
			max=a[i];
			if (a[i]<min)
			min=a[i];
			}                                    
		for(i=0;i<10;i++)                      
			printf("max=%d min=%d",max,min);
📌 ##输入10个整数，从小到大排序
#include<stdio.h>
int main()													//#define N 10
{															//int a[N], i, max, min;(推荐大写，可将 10 个换成其他数字，只需修改宏定义，不需要每个位置都替换。)
	int a[10],i,j,t;                       										
	int n=10;								
	for(i=0;i<n;i++)                          
		scanf("%d ",&a[i]);
	for(j=0;j<(n-1);j++)                      //排序n-1次
		for(i=0;i<(n-1-j);i++)
			if (a[i]>a[i+1])	                    //如果前面的数大
			{t=a[i];a[i]=a[i+1];a[i+1]=t;         //📌交换值：令t=前一个大数，把后面的小数值赋给前面，再把大数值t赋给后面
			}                                     //无else，前面数值小则不变
		for(i=0;i<10;i++)                       //输出10个数
			printf("%d ",a[i]);
	return 0;
}


